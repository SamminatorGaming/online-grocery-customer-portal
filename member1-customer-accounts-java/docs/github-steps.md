# GitHub steps for Member 1

Run these commands from the project folder.

```bash
git checkout -b member1-accounts-addresses
git add .
git commit -m "Add customer account and address features"
git push -u origin member1-accounts-addresses
```

Then open GitHub and create a pull request.
